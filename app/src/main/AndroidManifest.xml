<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dashhelper">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".update_plan" />
        <activity android:name=".add_plan" />
        <activity android:name=".view_plan" />
        <activity android:name=".donned_workouts" />
        <activity android:name=".swimming" />
        <activity android:name=".cycling" />
        <activity android:name=".walking" />
        <activity android:name=".running" />
        <activity android:name=".workouthome" />
        <activity android:name=".login" />
        <activity android:name=".singup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>